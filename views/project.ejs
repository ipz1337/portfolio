<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= project.title %> | Callum's Portfolio</title>
    <!-- Google Fonts -->
<link href="https://fonts.googleapis.com/css?family=Montserrat:400,700,800" rel="stylesheet">

<!-- Font Awesome -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.0/css/all.min.css">

<!-- Tailwind CSS -->
<link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.17/dist/tailwind.min.css" rel="stylesheet">

<style>
    body {
        font-family: "Montserrat", sans-serif;
        background-color: #1F2937;
        color: #F3F4F6;
        margin: 0;
        padding: 0;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 20px;
    }

    .main-heading {
        font-size: 4rem;
        font-weight: 800;
        line-height: 1.2;
        letter-spacing: -2px;
    }

    .sub-heading {
        font-size: 2rem;
        margin-top: 2rem;
    }

    .section-title {
        font-size: 3rem;
        font-weight: 800;
        line-height: 1.2;
        letter-spacing: -1px;
        margin-bottom: 2rem;
    }

    .project-details {
        font-size: 1.75rem;
        font-weight: 700;
        margin-top: 2rem;
    }

    .project-link {
        font-size: 1.75rem;
        font-weight: 700;
        color: #FBBF24;
        transition: color 0.3s ease-in-out;
    }

    .project-link:hover {
        color: #F3F4F6;
    }

    .project-image {
        object-fit: cover;
        filter: brightness(75%);
        transition: filter 0.3s ease-in-out;
        width: 100%;
        max-height: 60vh;
        border-radius: 8px;
    }

    .project-image:hover {
        filter: brightness(100%);
    }
    /* Set the width and color of the scrollbar */
::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background-color: #F3F4F6;
}

::-webkit-scrollbar-thumb {
    background-color: #1F2937;
  border-radius: 8px;
}

/* Style the scrollbar corner */
::-webkit-scrollbar-corner {
  background-color: #F3F4F6;
}
body {
  overflow-x: hidden;
}
</style>
</head>
<body class="bg-gray-900 text-white">
    <%- include('partials/navbar') %>
    <div class="container bg-gray-800">
        <!-- Navigation Bar Section -->
        
        <!-- Project Section -->
    <section class="py-20">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
            <div class="text-center md:text-left">
                <h1 class="main-heading"><%= project.title %></h1>
                <p class="sub-heading"><%= project.desc %></p>
            </div>
            <div>
                <img class="mx-auto md:mx-0 project-image" src="<%= project.image %>" alt="<%= project.title %>">
            </div>
        </div>
    </section>

    <section class="py-20">
        <h2 class="section-title">Project Details</h2>
        <p class="project-details">Technologies used: <%= project.techs_used %></p>
        <button class="project-link">
            <a href="<%= project.url %>" class="button-link">View Project</a>
          </button>
          
          <style>
            .project-link {
              display: inline-block;
              border: none;
              background-color: #3c56db;
              color: #fff;
              font-size: 1.5rem;
              font-weight: 700;
              padding: 0.75rem 1.5rem;
              border-radius: 8px;
              transition: background-color 0.3s ease-in-out;
              text-decoration: none;
            }
          
            .button-link {
              color: #fff;
              text-decoration: none;
            }
          
            .project-link:hover {
              background-color: #F59E0B;
            }
          </style>
          
      </section>

        <!-- Footer Section -->
        
    </div>
    <%- include('partials/footer') %>
</body>
</html>
