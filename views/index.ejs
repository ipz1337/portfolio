<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Callum's Portfolio</title>
  <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700,800" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.0/css/all.min.css">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.17/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://cdn.rawgit.com/michalsnik/aos/2.1.1/dist/aos.css" rel="stylesheet">

  <style>
    :root {
      --clr-primary: #FBBF24;
      --clr-bg: #111827;
      --clr-text: #F3F4F6;
      --trans-speed: 0.3s;
      --content-padding: 20px;
    }

    /* Custom CSS */
    * {
      transition: all var(--trans-speed) ease-in-out;
    }

    body {
      font-family: 'Montserrat', sans-serif;
      background: var(--clr-bg);
      color: var(--clr-text);
      overflow-x: hidden;
    }

    /* Scrolling */
    ::-webkit-scrollbar {
      width: 8px;
    }

    ::-webkit-scrollbar-track {
      background-color: #F3F4F6;
    }

    ::-webkit-scrollbar-thumb {
      background-color: #1F2937;
      border-radius: 8px;
    }

    ::-webkit-scrollbar-corner {
      background-color: #F3F4F6;
    }

    /* Styling Classes */
    .main-heading {
      font-size: 4rem;
      font-weight: 800;
      color: var(--clr-text);
      line-height: 1.2;
      letter-spacing: -2px;
    }

    .sub-heading {
      font-size: 2rem;
      color: var(--clr-text);
      margin-top: 2rem;
    }

    .section-title {
      font-size: 3rem;
      font-weight: 800;
      color: var(--clr-text);
      line-height: 1.2;
      letter-spacing: -1px;
      margin-bottom: 2rem;
    }

    .section-description {
      font-size: 1.5rem;
      color: var(--clr-text);
      margin-top: 2rem;
    }

    .project-title {
      font-size: 2.5rem;
      font-weight: 700;
      color: var(--clr-text);
      margin-bottom: 1rem;
    }

    .project-description {
      font-size: 1.5rem;
      color: var(--clr-text);
      margin-bottom: 2rem;
    }

    .project-link {
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--clr-text);
      transition: color var(--trans-speed) ease-in-out;
    }

    .project-link:hover {
      color: var(--clr-primary);
    }

    .footer {
      background-color: var(--clr-bg);
      color: var(--clr-text);
    }

    .footer p {
      font-size: 1.5rem;
    }

    .technologies {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 20px;
      margin: 0 auto;
      max-width: 800px;
      padding: var(--content-padding);
    }

    .technology {
      text-align: center;
      padding: var(--content-padding);
      border-radius: 10px;
      background-color: #2d3748;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
      transition: all var(--trans-speed) ease-in-out;
    }

    .technology:hover {
      transform: scale(1.05);
      box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.3);
    }

    .technology i {
      font-size: 3rem;
      margin-bottom: 20px;
    }

    .technology p {
      font-size: 1.5rem;
      margin-bottom: 0;
      font-weight: bold;
    }
  </style>
</head>

<body class="overflow-x-hidden">
  <%- include('partials/navbar') %>
  <div class="container mx-auto px-4">
    <!-- About Section -->
    <section class="bg-gray-800 text-center py-20" data-aos="fade-up">
      <h1 class="main-heading">About Me</h1>
      <p class="sub-heading">Hi, I'm Callum, a web developer. Welcome to my portfolio!</p>
    </section>
    <!-- Known Technologies Section -->
    <section class="bg-gray-800 text-center py-20" data-aos="fade-up" data-aos-delay="200">
      <h2 class="section-title">Known Technologies</h2>
      <p class="section-description">Here are some of the technologies I know:</p>
      <div class="technologies grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4">
        <div class="technology">
          <i class="fab fa-python"></i>
          <p>Python</p>
        </div>
        <div class="technology">
          <i class="fab fa-node-js"></i>
          <p>Node.JS</p>
        </div>
        <div class="technology">
          <i class="fab fa-php"></i>
          <p>PHP</p>
        </div>
        <div class="technology">
          <i class="fab fa-html5"></i>
          <p>HTML</p>
        </div>
        <div class="technology">
          <i class="fab fa-css3-alt"></i>
          <p>CSS</p>
        </div>
        <div class="technology">
          <i class="fab fa-golang"></i>
          <p>GoLang</p>
        </div>
        <div class="technology">
          <i class="fas fa-database"></i>
          <p>MYSQL</p>
        </div>
        <div class="technology">
          <i class="fas fa-database"></i>
          <p>Mongodb</p>
        </div>
        <div class="technology">
          <i class="fab fa-windows"></i>
          <p>Windows</p>
        </div>
        <div class="technology">
          <i class="fab fa-linux"></i>
          <p>Linux</p>
        </div>
      </div>
    </section>
    <!-- Projects Section -->
    <section data-aos="fade-up" data-aos-delay="400">
      <%- include('partials/projectsIndex') %>
    </section>
    <!-- Contact and Footer Section -->
  </div>
  <section data-aos="fade-up" data-aos-delay="800">
    <%- include('partials/footer') %>
  </section>

  <script src="https://cdn.rawgit.com/michalsnik/aos/2.1.1/dist/aos.js"></script>
  <script>
    AOS.init({
      duration: 1500,
      once: true
    });
    
  </script>
</body>

</html>
