<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Callum's Portfolio</title>
    <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700,800" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.0/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.17/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdn.rawgit.com/michalsnik/aos/2.1.1/dist/aos.css" rel="stylesheet">
  
    <style>
      :root {
        --clr-primary: #FBBF24;
        --clr-bg: #111827;
        --clr-text: #F3F4F6;
        --trans-speed: 0.3s;
        --content-padding: 20px;
      }
  
      /* Custom CSS */
      * {
        transition: all var(--trans-speed) ease-in-out;
      }
  
      body {
        font-family: 'Montserrat', sans-serif;
        background: var(--clr-bg);
        color: var(--clr-text);
        overflow-x: hidden;
      }
  
      /* Scrolling */
      ::-webkit-scrollbar {
        width: 8px;
      }
  
      ::-webkit-scrollbar-track {
        background-color: #F3F4F6;
      }
  
      ::-webkit-scrollbar-thumb {
        background-color: #1F2937;
        border-radius: 8px;
      }
  
      ::-webkit-scrollbar-corner {
        background-color: #F3F4F6;
      }
  
      /* Styling Classes */
      .main-heading {
        font-size: 4rem;
        font-weight: 800;
        color: var(--clr-text);
        line-height: 1.2;
        letter-spacing: -2px;
      }
  
      .sub-heading {
        font-size: 2rem;
        color: var(--clr-text);
        margin-top: 2rem;
      }
  
      .section-title {
        font-size: 3rem;
        font-weight: 800;
        color: var(--clr-text);
        line-height: 1.2;
        letter-spacing: -1px;
        margin-bottom: 2rem;
      }
  
      .section-description {
        font-size: 1.5rem;
        color: var(--clr-text);
        margin-top: 2rem;
      }
  
      .project-title {
        font-size: 2.5rem;
        font-weight: 700;
        color: var(--clr-text);
        margin-bottom: 1rem;
      }
  
      .project-description {
        font-size: 1.5rem;
        color: var(--clr-text);
        margin-bottom: 2rem;
      }
  
      .project-link {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--clr-text);
        transition: color var(--trans-speed) ease-in-out;
      }
  
      .project-link:hover {
        color: var(--clr-primary);
      }
  
      .footer {
        background-color: var(--clr-bg);
        color: var(--clr-text);
      }
  
      .footer p {
        font-size: 1.5rem;
      }
  
      .technologies {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
        gap: 20px;
        margin: 0 auto;
        max-width: 800px;
        padding: var(--content-padding);
      }
  
      .technology {
        text-align: center;
        padding: var(--content-padding);
        border-radius: 10px;
        background-color: #2d3748;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
        transition: all var(--trans-speed) ease-in-out;
      }
  
      .technology:hover {
        transform: scale(1.05);
        box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.3);
      }
  
      .technology i {
        font-size: 3rem;
        margin-bottom: 20px;
      }
  
      .technology p {
        font-size: 1.5rem;
        margin-bottom: 0;
        font-weight: bold;
      }
    </style>
  </head>
<body class="overflow-x-hidden">
    <%- include('partials/navbar') %>
    <div class="container mx-auto px-4 py-20">
        <h2 class="section-title">Blogs</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% blogs.forEach(blog => { %>
                <div class="bg-gray-800 p-6 rounded-lg">
                    <h3 class="project-title"><%= blog.title %></h3>
                    <p class="text-sm text-gray-400"><%= blog.date %></p>
                    <img src="https://via.placeholder.com/500x300" alt="blog image" class="rounded-lg mt-4">

                    <a href="/blogs/<%= blog.id %>" class="mt-4 inline-block bg-yellow-500 text-black py-2 px-4 rounded hover:bg-yellow-600">Read More</a>
                </div>
            <% }) %>
        </div>
        <div class="mt-10">
            <a href="/blogs?page=<%= prevPage %>" class="px-4 py-2 bg-yellow-500 text-black rounded-l hover:bg-yellow-600">Previous</a>
            <a href="/blogs?page=<%= nextPage %>" class="px-4 py-2 bg-yellow-500 text-black rounded-r hover:bg-yellow-600">Next</a>
        </div>
    </div>
    <%- include('partials/footer') %>
    <!-- ... (same as your provided scripts) ... -->
    <script src="https://cdn.rawgit.com/michalsnik/aos/2.1.1/dist/aos.js"></script>
  <script>
    AOS.init({
      duration: 1500,
      once: true
    });
    
  </script>
</body>
</html>
